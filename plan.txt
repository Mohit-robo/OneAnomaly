I want to create a anomaly detection web app. So the idea is that the user uploads a zip file with all the good images of a part/ industrial job. So we extract the patch-wise features of all the good images and save them, so the option here is either used the features from the current session or previously saved features. 
Now the user has the option to upload not okay images, either folder, zip file or a single image, if the user has uploaded a single image, show the image on the web page and give option to save localy. But if the user uploads a folder or zip file, extract the zip file and then process all the not okay images. Then perform a similarity search using faiss-gpu library, then create anomaly maps on the original image like this https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR7ac6POMxBBfGtVr_gCiM-SDa7SlOyT1hJVw&s. Then  save the entire folder for that run with the unique session name name. For all of this, give proper buttons not too fancy look but a decent one with black and white theme with the background white.

Now What tools are we uisng. 

1. For feature extraction use dinov3, make sure you check the github repo : https://github.com/facebookresearch/dinov3. Use ViT-S/16 distilled pytorch weights. So for this we need proper testing scripts, I will verify them manually. First we start with pytorch testing by giving one folder as input to run a simple python script, check the image outputs. Ultimately we want to run the model in tensorrt format so then we will convert the model to tensorrt and test those weights performance manually. Then we start with the building of the web app. Now the other thing I want you to try is understand how this repo uses the memory bank to store the good features https://github.com/amazon-science/patchcore-inspection and see if we can implement this and verify which is more efficient

2. Web App: I want to create a production web app. So I want you to use nodejs. I dont have any knowledge about nodejs so you have to manage that testing, make sure you are clear about what you are outputting. Make sure you handle how the standard folder structure while uploading the folders is. Use proper alingnments for uploading button. Create a proper button to start feature extraction of good images then, save the extracted features locally, in a pickle file or something to load later on, for that create a input box for the user to enter a filename so that the user can select it in the later run. 
So there should be and option if the user wants to upload new folder or load previous features. Then upload the not okay image folder. Make a standard folder for structure for this as well.

Lets do this and then make changes as required. Create the required files and folders you have the permisssion.

So if you want the app reference you can check this web url https://switchon.io/product/ 
I want to create something like this but lets start small.

--------------------------------------------------------------

* Show a progress bar on the web page when extracting features and searching or processing for anomaly. Could also try a percentage bar, or a clock. Try your own thinking as well, but try to show something AI centric design.

* Initializing feature extractor...
Loading dinov3_vits16 with PyTorch backend...
Loading from local weights: dinov3_vits16_pretrain_lvd1689m.pth
Model loaded: patch_size=16, embed_dim=384
Initializing memory bank...
Models initialized successfully!
Loading memory bank from D:\Mohit\anomaly_app\memory_banks\verified_mb.pkl...
Building FAISS index...
Using IndexFlatIP (Cosine Similarity)
FAISS index built with 117248 vectors
Memory bank loaded successfully
  Features: (117248, 384)

Show all these lines on UI as updates so that we dont test the user's patience. So update the web page to show a terminal type thing showing all the logs that actually print but only during infernec or feature extraction


* Instead of these updates : Loaded successfully! 117248 features (dim: 384)
    during loading memory bank and after loading remove this create a alarm popup for such incidents throught the app.

* Add a vector database to save the feature vectors and perform seach if efficient.

--------------------------------------------------------------

Improvement Points:
